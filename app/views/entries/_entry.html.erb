  <th scope="row"><%= link_to "#{entry.name}", entry_path(entry) %></th>
  <td>$ <%= entry.amount %></td>
  <td><% entry.categories.each do |category| %>
    <%= link_to category.name, category_path(category) %>
  <% end %>
  </td>
